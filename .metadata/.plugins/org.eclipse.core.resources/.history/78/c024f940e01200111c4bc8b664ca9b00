package com.labs;

import java.util.stream.Collectors;

public class EmployeeService {
	public double sumOfSalary() {
		double totalSal = EmployeeRepo.getEmployees().stream().collect(Collectors.summingDouble(e->e.getSalary()));
		return totalSal;
	}
}
